Phil John
Colin Dunn

Python KNN implementation

Predicts the label of a test instance based on K neighbors & choice of distance metric (Euclidean, L2, and Max Norm).

Input: python knearest.py --K (# of neighbors) --method (L1,L2,Linf)

Outputs the predicted label for each test instance. 